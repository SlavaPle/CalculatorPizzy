# 🧮 Kalkulator - Универсальный калькулятор

Монорепо для создания универсального калькулятора с поддержкой сложных математических формул, единиц измерения и веб-интерфейса.

## 🏗️ Архитектура

```
kalkulator/
├── web/                 # React веб-приложение
├── backend/            # Node.js API сервер
├── shared/             # Общие компоненты и типы
├── nginx/              # Nginx конфигурация
├── scripts/            # Скрипты для разработки
├── docker-compose.yml  # Docker Compose конфигурация
└── package.json        # Монорепо конфигурация
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Клонирование репозитория
git clone <repository-url>
cd kalkulator

# Установка всех зависимостей
npm run install:all

# Или автоматическая настройка
node scripts/setup.js
```

### 2. Настройка окружения

```bash
# Копирование .env файлов
cp backend/env.example backend/.env

# Настройка переменных окружения
# Отредактируйте backend/.env файл
```

### 3. Запуск базы данных

```bash
# Через Docker Compose
npm run docker:up

# Или локально
# PostgreSQL на порту 5432
# Redis на порту 6379
```

### 4. Настройка базы данных

```bash
# Генерация Prisma клиента
npm run db:generate

# Применение миграций
npm run db:push

# Открытие Prisma Studio
npm run db:studio
```

### 5. Запуск приложения

```bash
# Запуск всех сервисов одновременно
npm run dev

# Или через Docker
npm run docker:up
```

## 📋 Доступные команды

### Основные команды

```bash
# Разработка
npm run dev              # Запуск всех сервисов
npm run dev:web          # Только веб-приложение
npm run dev:backend      # Только API сервер

# Production
npm run start            # Запуск production серверов
npm run build            # Сборка всех проектов

# Установка
npm run install:all      # Установка всех зависимостей
npm run setup            # Полная настройка проекта
```

### База данных

```bash
npm run db:generate      # Генерация Prisma клиента
npm run db:push          # Применение изменений схемы
npm run db:migrate       # Создание миграций
npm run db:studio        # Открытие Prisma Studio
npm run db:seed          # Заполнение тестовыми данными
```

### Docker

```bash
npm run docker:up        # Запуск через Docker
npm run docker:down      # Остановка Docker контейнеров
npm run docker:build     # Сборка Docker образов
npm run docker:logs      # Просмотр логов
```

### Линтинг и тестирование

```bash
npm run lint             # Проверка кода
npm run lint:fix         # Автоисправление
npm run test             # Запуск тестов
npm run type-check       # Проверка типов
```

### Очистка

```bash
npm run clean            # Очистка всех проектов
npm run clean:web        # Очистка веб-приложения
npm run clean:backend    # Очистка API сервера
npm run clean:shared     # Очистка общих компонентов
```

## 🌐 Доступные URL

После запуска приложения доступны следующие URL:

- **Web App**: http://localhost:3000
- **API**: http://localhost:3001
- **Health Check**: http://localhost:3001/api/health
- **Prisma Studio**: http://localhost:5555 (если запущен)

## 🐳 Docker Development

### Запуск через Docker Compose

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### Сервисы Docker

- **PostgreSQL**: порт 5432
- **Redis**: порт 6379
- **Backend API**: порт 3001
- **Web App**: порт 3000
- **Nginx**: порт 80

## 🔧 Настройка разработки

### Переменные окружения

Создайте файл `backend/.env` на основе `backend/env.example`:

```env
# Server
NODE_ENV=development
PORT=3001
HOST=localhost

# Database
DATABASE_URL="postgresql://username:password@localhost:5432/kalkulator_db"

# Redis
REDIS_URL="redis://localhost:6379"

# JWT
JWT_SECRET="your-super-secret-jwt-key-here"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-here"

# CORS
CORS_ORIGIN="http://localhost:3000"
```

### База данных

1. Установите PostgreSQL
2. Создайте базу данных `kalkulator_db`
3. Настройте `DATABASE_URL` в `.env`
4. Выполните `npm run db:push`

### Redis

1. Установите Redis
2. Запустите Redis сервер
3. Настройте `REDIS_URL` в `.env`

## 📁 Структура проекта

### Web (React + TypeScript)
- **Vite** - сборщик
- **Material-UI** - компоненты
- **Redux Toolkit** - состояние
- **React Router** - маршрутизация

### Backend (Node.js + TypeScript)
- **Express** - веб-сервер
- **Prisma** - ORM
- **PostgreSQL** - база данных
- **Redis** - кэширование
- **JWT** - аутентификация

### Shared (TypeScript)
- **Модели данных** - общие типы
- **Формулы** - движок вычислений
- **Единицы измерения** - конвертация
- **UI компоненты** - переиспользуемые

## 🚀 Production Deployment

### Сборка

```bash
# Сборка всех проектов
npm run build

# Сборка отдельных проектов
npm run build:web
npm run build:backend
npm run build:shared
```

### Запуск

```bash
# Production серверы
npm run start

# Или через Docker
docker-compose -f docker-compose.prod.yml up -d
```

## 🛠️ Разработка

### Добавление новых функций

1. Создайте ветку: `git checkout -b feature/new-feature`
2. Внесите изменения в соответствующий проект
3. Обновите общие типы в `shared/`
4. Протестируйте изменения
5. Создайте Pull Request

### Структура API

- `GET /api/health` - проверка здоровья
- `POST /api/auth/login` - вход
- `POST /api/auth/register` - регистрация
- `GET /api/calculators` - список калькуляторов
- `POST /api/calculators` - создание калькулятора
- `GET /api/formulas` - список формул
- `POST /api/calculations/execute` - выполнение расчета

## 📚 Документация

- [API Documentation](./docs/api.md)
- [Database Schema](./docs/database.md)
- [Deployment Guide](./docs/deployment.md)
- [Contributing Guide](./docs/contributing.md)

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл [LICENSE](./LICENSE)

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте [Issues](https://github.com/your-repo/issues)
2. Создайте новый Issue
3. Опишите проблему подробно

---

**Создано с ❤️ для универсальных вычислений**